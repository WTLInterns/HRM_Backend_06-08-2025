spring.application.name=demo
server.address=0.0.0.0
server.port=8081
# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/managifyhrdb
spring.datasource.username=root
spring.datasource.password=Password@181298
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool Settings
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${MAIL_USERNAME:your_email@gmail.com}  
spring.mail.password=${MAIL_PASSWORD:your_app_password}
spring.mail.protocol=smtps
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=true

# Firebase Configuration (Environment Variables)
firebase.type=${FIREBASE_TYPE:service_account}
firebase.project-id=${FIREBASE_PROJECT_ID:notification-bb346}
firebase.private-key-id=${FIREBASE_PRIVATE_KEY_ID:}
firebase.private-key=${FIREBASE_PRIVATE_KEY:}
firebase.client-email=${FIREBASE_CLIENT_EMAIL:}
firebase.client-id=${FIREBASE_CLIENT_ID:}
firebase.auth-uri=${FIREBASE_AUTH_URI:https://accounts.google.com/o/oauth2/auth}
firebase.token-uri=${FIREBASE_TOKEN_URI:https://oauth2.googleapis.com/token}
firebase.auth-provider-x509-cert-url=${FIREBASE_AUTH_PROVIDER_X509_CERT_URL:https://www.googleapis.com/oauth2/v1/certs}
firebase.client-x509-cert-url=${FIREBASE_CLIENT_X509_CERT_URL:}
firebase.universe-domain=${FIREBASE_UNIVERSE_DOMAIN:googleapis.com}

# Google Maps API Configuration
google.maps.api.key=${GOOGLE_MAPS_API_KEY:AIzaSyCelDo4I5cPQ72TfCTQW-arhPZ7ALNcp8w}

# File Upload Settings - Support for ultra-high resolution mobile images
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=200MB

# EasyTimePro Configuration
easytime.api.base-url=http://127.0.0.1:8081
easytime.api.username=admin
easytime.api.password=Admin@123
easytime.webhook.enabled=true
easytime.sync.interval=30000

# ZKTeco TCP Listener Configuration
zkteco.listener.ports=4370,8000,8080,8181,8081
zkteco.listener.enabled=true
zkteco.listener.bind-address=0.0.0.0

# Enhanced Real-Time Biometric Sync Configuration
biometric.enhanced.enabled=true
biometric.enhanced.sync-interval=10000
biometric.enhanced.max-transactions-per-sync=100
biometric.enhanced.duplicate-detection=true
biometric.enhanced.comprehensive-logging=true

# Multi-Device & Multi-Subadmin Configuration
biometric.multidevice.enabled=true
biometric.multidevice.device-isolation=true
biometric.multidevice.subadmin-context=true
biometric.multidevice.employee-id-conflict-resolution=true

# Employee Mapping Strategies (in order of priority)
biometric.mapping.strategy1=device-mapping-table
biometric.mapping.strategy2=machine-empid-with-subadmin
biometric.mapping.strategy3=empcode-parsing
biometric.mapping.strategy4=direct-empid-lookup

# Data Source Configuration
biometric.datasource.iclock-table=easywdms.iclock_transaction
biometric.datasource.attendance-table=managifyhrdb.attendance
biometric.datasource.device-mapping-table=managifyhrdb.employee_device_mapping
biometric.datasource.terminal-table=managifyhrdb.subadmin_terminals

# Processing Configuration
biometric.processing.batch-size=50
biometric.processing.error-retry-attempts=3
biometric.processing.transaction-timeout=30
biometric.processing.cache-processed-transactions=true

# Real-time Notifications
biometric.notifications.enabled=true
biometric.notifications.websocket-topics=/topic/attendance,/topic/biometric
biometric.notifications.include-device-info=true
biometric.notifications.include-employee-details=true

# Monitoring and Debugging
biometric.monitoring.enabled=true
biometric.monitoring.log-unmapped-employees=true
biometric.monitoring.log-device-stats=true
biometric.monitoring.log-processing-time=true

# Allow bean definition overriding
spring.main.allow-bean-definition-overriding=true

spring.web.resources.static-locations=file:src/main/resources/static/,file:src/main/resources/

file.upload-dir=src/main/resources/static/uploads

